      setIsGeneratingPaper(false);
    }
  };

  const generateFullAssessment = () => generateAssessmentPaper('Full');
  const generateTopicalAssessment = () => generateAssessmentPaper('Topical');

  const compressImage = async (base64: string, maxWidth = 800, maxHeight = 800, quality = 0.7): Promise<string> => {
    return new Promise((resolve) => {
      const img = new Image();
      img.onload = () => {
--
                           {isGeneratingPaper ? 'Compiling Paper...' : 'Full Assessment Paper'}
                         </button>

                         <button
                           disabled={selectedTopics.length === 0 || isGenerating || isGeneratingPaper}
                           onClick={() => generateAssessmentPaper('Topical')}
                           className="px-6 py-3.5 bg-slate-900 text-white rounded-2xl font-black uppercase tracking-[0.15em] text-[10px] disabled:opacity-30 transition-all hover:bg-slate-800 hover:scale-105 active:scale-95 shadow-xl shadow-slate-900/40 flex items-center justify-center gap-2.5 min-w-[140px] border border-white/20"
                         >
                           {isGeneratingPaper ? <Loader2 className="w-4 h-4 animate-spin text-white" /> : <Target className="w-4 h-4 text-brand-400" />}
                           {isGeneratingPaper ? 'Analyzing Topic...' : 'Topical Masterclass'}
                         </button>
